# 设计文档：国家公园打卡工具站（代号 ParkQuest）

> 基于 `hypotheses.md` 的假设与 `reference/full_gefei.md` 的调研结论。
> 本文档是本轮 MVP 的实现依据——功能范围刻意收窄，只验证"游戏化差异化"和"页面池结构"两个核心假设。

---

## 一、产品定位

**一句话**：把"集齐 63 个国家公园"这件事，从一张纸质护照册的打勾，变成一个有进度、有成就、能坚持下去的线上收集游戏。

**目标用户**（对应 [[hypotheses]] 假设 5）：
- 主用户：计划型的成年收集者——已经在买实体护照册、专门跑去公园盖章的人，需要一个随时能看"我还差几个"的地方。
- 次用户：带娃自驾公园的家长——为 Junior Ranger 活动付费意愿最高（CPC $0.85~$3.77），需要一个能和孩子一起打卡、有成就感反馈的工具。
- 明确不做：青少年即点即玩人群（搜索侧不存在这个需求，他们去 Poki/CrazyGames）。

**差异化**：6 个在跑竞品清一色只做"记录"（checklist/tracker/map），无一家做游戏化。我们加成就系统、连续打卡、进度可视化，把"记录"变成"游戏"。

---

## 二、MVP 范围（这次要做的，明确 in / out）

### In（本次构建）
1. **63 个美国国家公园清单**（NPS 官方 63 个 National Park 单元，不含 433 个全部 NPS 单元——留到 V1）
2. **打卡交互**：点击标记"已去过"，记录访问日期（可选填）
3. **进度可视化**：总进度条（X/63，百分比）、按地区（West/Midwest/Northeast/Southeast/Alaska/territories）分组的完成度
4. **成就系统**：里程碑徽章（第1个、5个、10个、25个、50个、63个全部）+ 地区徽章（集齐某个地区）
5. **连续打卡（streak）**：基于用户"标记访问"的操作日期计算连续天数，鼓励持续互动（不要求真的连续几天去公园，而是连续几天来网站互动/规划）
6. **本地持久化**：localStorage 保存进度，不做账号系统
7. **响应式静态站**：无后端、无构建依赖的纯静态 HTML/CSS/JS，直接可部署 Cloudflare Pages
8. **为联盟位预留但不接真实链接**：护照本/地图周边的展示卡片位置，MVP 阶段用占位文案

### Out（明确排除，留到后续版本）
- 433 个 NPS 全单元 + 州立公园（假设 3 的完整验证，V1 做）
- 一词一页的 8 套变体 URL 矩阵（checklist / tracker / counter / map 独立 URL，V1 做，SEO 收益最大但 MVP 先验证产品本身）
- 账号系统/云同步（数据目前只在本地浏览器，换设备会丢失——V2 视留存数据决定是否做）
- 真实联盟链接和 AdSense 接入（先有流量再谈变现）
- GPS 打卡验证（parkchive.com 已经在做，非差异化重点）
- Quiz/trivia 模块（调研里提到但量太小，仅作为未来内页候选）

---

## 三、核心交互与游戏化机制

### 3.1 打卡（Check-in）
- 每个公园卡片：名称、所在州、建立年份、一句话简介、缩略图（用 emoji/图标占位，避免版权风险）
- 点击卡片 → 切换"已访问"状态 → 弹出访问日期选择（默认今天，可留空/编辑）
- 已访问的卡片视觉上做出区分（描边高亮 + 一个小图章样式的勾选标记，呼应"护照盖章"的心智）

### 3.2 进度可视化
- 顶部固定进度条：`X / 63 已收集`，配色随进度从浅绿到深绿过渡
- 按地区分组的迷你进度环（West / Southwest / Midwest / Northeast / Southeast / Alaska / Hawaii & Territories）
- 一个"下一个建议目标"模块：随机推荐一个未打卡且离已打卡公园地理位置近的公园（用简单的地区就近规则，不做真实地图算路）

### 3.3 成就系统（Achievements）
| 徽章 | 触发条件 |
|---|---|
| 初次出发 | 打卡 1 个公园 |
| 收集新手 | 打卡 5 个 |
| 公园探索者 | 打卡 10 个 |
| 公园达人 | 打卡 25 个 |
| 公园大师 | 打卡 50 个 |
| 63 大满贯 | 打卡全部 63 个 |
| 地区通关（×7） | 集齐某个地区的全部公园 |
| 连续 3 天 / 7 天 / 30 天 | streak 达标 |

### 3.4 连续打卡（Streak）
- 基于"用户在网站上产生打卡操作"的自然日计数，中断则清零，最高纪录单独保留展示（"最长连续 N 天"）
- 这是游戏化里成本最低、对留存验证价值最高的机制，MVP 优先做

---

## 四、信息架构（为 SEO 变体矩阵预留结构，MVP 先单页承载）

参照领跑者"一词一页"打法（[[hypotheses]] 事实 4），目标词表：

| 变体 | 目标词（示例） | MVP 阶段 | V1 阶段 |
|---|---:|---|---|
| 主清单 | national parks checklist | `/` 首页承载 | 保留 |
| 地图版 | national park map checklist | 首页内嵌地图视图 tab | 独立 `/map` |
| 打卡器 | national park tracker | 首页即是 tracker | 独立 `/tracker` 落地页，内容差异化措辞 |
| 计数器 | national park counter | 首页进度条即承载 | 独立 `/counter` |
| 护照 | national park passport stamps | 首页视觉隐喻 | 独立 `/passport` 详解页 |
| 单公园页 | {公园名} national park | 不做 | 63 个独立页面 `/parks/{slug}` |

MVP 阶段所有变体语义都压缩进首页的不同模块/锚点，用不同的 `<h2>` 和文案覆盖核心词，避免一次性铺开导致内容单薄（哥飞：内容不够会拖累排名）。V1 再拆分独立 URL。

---

## 五、视觉设计要点

- **主题色**：大地色系 + 国家公园标志性森林绿（主色 `#2d5a3d` 左右）、辅助土黄/赭石色（呼应 NPS 棕色路牌），进度类用渐变绿
- **护照隐喻**：已打卡状态用"盖章"视觉（圆形徽章/印章质感），强化"收集护照"的产品心智
- **响应式**：手机端单列卡片网格，桌面端多列网格，进度条和成就横幅始终可见
- **深浅色模式**：跟随系统偏好，深色模式用深绿/墨色背景，避免纯黑

---

## 六、技术架构

- **纯静态站**：HTML + CSS + 原生 JavaScript，零构建工具、零框架依赖——最快能部署到 Cloudflare Pages，且未来拆分多 URL 时每个页面独立可控，利于 SEO
- **数据来源**：`parks.json` 静态数据文件（63 个公园：名称、州、地区、建立年份、slug、简介）
- **状态存储**：`localStorage`，schema：
  ```json
  {
    "visited": { "yellowstone": "2024-07-12", "zion": "2025-03-01" },
    "achievements": ["first-checkin", "collector-5"],
    "streak": { "current": 3, "best": 7, "lastActionDate": "2026-09-15" }
  }
  ```
- **无后端**：MVP 不需要服务器逻辑，Cloudflare Pages 纯静态托管即可
- **未来扩展点**（V2，非本次范围）：账号系统可用 Cloudflare Workers + D1/KV 做云同步，无需推翻现有前端结构

---

## 七、SEO 与内容策略

- 首页 `<title>`：覆盖核心词组合（checklist / tracker 语义），`<h1>` 用主词 "National Parks Checklist & Tracker"
- 每个公园卡片自带简短文字描述（非纯交互组件），满足"游戏/工具页也要把文字铺够"的社群经验
- 后续拆分变体页面时，每个页面独立标题、独立 H1、内容措辞差异化，不做简单复制
- 域名策略（对应 [[hypotheses]] 假设 4）：优先选含 `park` + `checklist`/`tracker`/`passport` 词根的可用域名，不用抽象品牌名；本轮部署先用 Cloudflare Pages 提供的 `*.pages.dev` 子域名验证产品，正式域名待扫描注册状态后再绑定

---

## 八、变现规划（分阶段，不在 MVP 内实现）

1. **MVP**：不接变现，只验证产品是否被使用、游戏化机制是否提升停留和回访
2. **V1**：AdSense 接入 + 联盟卡片（护照本、刮刮乐地图、公园海报——对应 [[hypotheses]] 事实 8 里出价最高的电商词）
3. **V2**：视流量规模决定是否做账号系统+邮件订阅（复访召回），提升联盟转化

---

## 九、成功指标（对应 hypotheses.md 的验证方式）

| 指标 | 对应假设 | MVP 阶段目标 |
|---|---|---|
| 部署可访问、核心交互无 bug | — | 上线即达标 |
| 人均访问页数 / 停留时长 | 假设1 | 建立基线，之后对比竞品公开数据 |
| streak 触发率（有多少用户产生连续打卡行为） | 假设1、假设5 | 建立基线 |
| localStorage 留存（回访时进度仍在） | 假设5 | 定性验证产品心智是否成立 |

---

## 十、路线图

- **本轮（MVP）**：单页应用，63 公园清单 + 打卡 + 进度 + 成就 + streak，部署到 GitHub + Cloudflare Pages
- **V1（验证后）**：拆分变体矩阵 URL、补 433 个 NPS 单元、接入真实联盟链接和 AdSense、正式域名
- **V2（有流量后）**：账号云同步、Quiz 模块、海报类目变现测试（[[hypotheses]] 边界 3）
